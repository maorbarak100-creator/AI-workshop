<div dir="rtl">

# Instructor Setup: Testers

תיקייה זו **למדריך בלבד**, תחת `exercises/instructor-setup/testers/`.  
היא **לא** חלק מזרימת המשתתף בפלייבוק.

כאן נמצא חומר להקמת סביבת Azure DevOps / החלטות MCP לפני הכיתה.

| קובץ | תפקיד |
|------|--------|
| `DEMO-SETUP.md` | מה זה Org/MSP/TestPlan, הקמת פרויקט דמו, באגים, checklist |
| `source-notes/` | טיוטות מקור לכרטיסי התרגיל (לא לפתוח בכיתה) |

## מה נשאר בערכת המשתתפים

ב-`../../ai-for-testers/`:

- `playbook.html`, `README.md`
- `fixtures/` (אפיון, תבנית מייל, CSV ייבוא, offline status)

המשתתפים פותחים את `ai-for-testers` ב-Cursor ואת `playbook.html` בדפדפן. הם לא צריכים את `instructor-setup/`.

</div>
